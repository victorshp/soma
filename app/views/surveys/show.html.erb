<div class="container w-25">
  <div class="row">
    <div class="col text-jutify">
      <h1 class='m-3 form text-center'><%= @survey.title %></h1>
      <% @survey.questions.each do |question| %>
        <%= simple_format(question.content) %>
      <% end %>
    </div>
  </div>
</div>
