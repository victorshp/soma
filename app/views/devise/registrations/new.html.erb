<%= javascript_tag do %>
  document.querySelector('.nav_bar').remove()
<% end %>

<div class="container w-50">
  <div class="row">
    <div class="col ">
      <div class="card border-light my-5">
        <div class="card-header text-center" style="background-color: #424874; color: white;"><h2>Sign up</h2></div>
          <div class="card-body">
             <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
               <%= f.error_notification %>

               <div class="form-inputs">

                 <%= f.input :email,
                             required: true,
                             autofocus: true,
                             placeholder: "Email",
                             label: false,
                             input_html: { autocomplete: "email" } %>
                 <%= f.input :password,
                             required: true,
                             placeholder: "Password",
                             label: false,
                             hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                             input_html: { autocomplete: "new-password" } %>
                 <%= f.input :password_confirmation,
                             required: true,
                             placeholder: "Confirm Password",
                             label: false,
                             input_html: { autocomplete: "new-password" } %>
                 <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
               </div>

               <div class="form-actions">
                 <%= f.button :submit, "Sign up" , class:"btn-green" %>
               </div>
             <% end %>

             <%= render "devise/shared/links" %>
        </div>
      </div>
    </div>
  </div>
</div>


<!-- <div class="container">
  <div class="row justify-content-center margin-bg">
    <div class="col-5 sign-up">
      <div class="row justify-content-center">
        <%#= image_tag "https://res.cloudinary.com/dauxxi1io/image/upload/v1583527320/soma_logo_placeholder_vyxzdx.png", class: "logo-sign" %>    
      </div>

      <div class="row justify-content-left">
        <h1 class='mb-3' style="font-size: 2rem;margin-left: 6vh;">Create Account</h1>
      </div>

      <div class="row justify-content-center">
        <%#= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
           <%#= f.error_notification %>
           <%#= f.input :email,
                         required: true,
                         autofocus: true,
                         placeholder: "Email",
                         label: false,
                         input_html: { autocomplete: "email", style: 'width: 20vw'} %>
             <%#= f.input :password,
                         required: true,
                         placeholder: "Password",
                         label: false,
                         hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                         input_html: { autocomplete: "new-password" } %>
             <%#= f.input :password_confirmation,
                         required: true,
                         placeholder: "Confirm Password",
                         label: false,
                         input_html: { autocomplete: "new-password" } %>
          </div>
          
          <div class="row justify-content-center">
            <%#= f.button :submit, "Login", class:"btn-green" %>
          <%# end %>
          </div>
        
          <div class="mt-5 row justify-content-center" style='font-size: 1rem;'>
            <%#- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
              <%#= link_to "Forgot my password", new_password_path(resource_name) %><br />
            <%# end %>
          </div>

    </div>
  </div>
</div> -->
